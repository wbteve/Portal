<?php

/**
 * Implements hook_entity_info_alter().
 * Add a view mode for nodes placed in a dossier.
 */
function bg_dossier_entity_info_alter(&$entity_info) {
  $entity_info['node']['view modes']['dossier'] = array(
    'label' => t('In dossier'),
    'custom settings' => TRUE,
  );
} 
